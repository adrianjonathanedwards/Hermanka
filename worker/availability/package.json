{
  "name": "hermanka-availability",
  "version": "0.1.0",
  "private": true,
  "description": "GET /api/availability   e-chalupy iCal feed to booked date ranges. Deployed separately from the Astro site.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260919.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.135.0"
  }
}
